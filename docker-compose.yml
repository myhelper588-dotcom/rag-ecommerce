services:
  rag-app:
    build: .
    container_name: rag-ecommerce
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    env_file:
      - .env
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true